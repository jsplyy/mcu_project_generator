/*!
@page driver_log Driver Change Log

@section dac32 DAC32
  Current DAC32 driver version is 2.0.1

  - 2.0.0
    - Initial version.
  - 2.0.1
    - Bug fix:
      - Move the default DAC32_Enable(..., true) from DAC32_Init() to application code, so that users can enable the DAC's output when they will. 
*/
